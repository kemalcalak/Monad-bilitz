{
    "name": "signature-authority-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for Signature Move Authority System on Monad",
    "main": "index.js",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy": "hardhat run scripts/deploy.js --network monad_testnet",
        "deploy:local": "hardhat run scripts/deploy.js --network localhost"
    },
    "keywords": [
        "blockchain",
        "monad",
        "smart-contracts",
        "solidity"
    ],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.28.4"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^17.2.3"
    }
}
